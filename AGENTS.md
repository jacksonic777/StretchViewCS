## ファイル操作に関する文字コードと改行コード

- パスの取得やファイル操作は**必ず**PowerShellで行う。
- ファイルやフォルダのパスやテキストファイルの中身を扱う場合、以下の文字コードと改行コードを利用する。
  - PowerShellのコンソール上でのパスや文字の表示：UTF-8, LF。
  - CSVファイル：BOM付きUTF-8, CRLF。
  - Markdown、YAML、TOML、Textなどテキストベースファイル：BOM無しUTF-8, LF。
  - PowerShell5.xのソースファイル：BOM付きUTF-8, LF。
  - PowrShell7.xのソースファイル：BOM無しUTF-8, LF。

  AI エージェント向け作業指示（PowerShell 5.1 / NoProfile）

- 適用範囲: このファイルが置かれたフォルダ配下すべて。

- UTF-8 実行ラッパ（必須・毎回付与）
  - 形式（`<COMMAND>` を実コマンドに置換）:
    - `[Console]::InputEncoding=[Text.UTF8Encoding]::new($false); [Console]::OutputEncoding=[Text.UTF8Encoding]::new($false); $OutputEncoding=[Text.UTF8Encoding]::new($false); chcp 65001 > $null; & { <COMMAND> }`

- ファイル書き込みの Encoding 指定（必須）
  - `Out-File -Encoding utf8` / `Set-Content -Encoding utf8` / `Add-Content -Encoding utf8`

## 文字化け対策

- このリポジトリには、UTF-8 と CP932/Shift_JIS が混在している可能性がある。日本語コメントを含む既存ファイルは、編集前に文字コード混在を疑う。
- 文字コードが不明な既存ファイルは、内容確認だけであっても、読み取りと保存で別エンコーディングを混ぜない。
- 日本語を含むファイルは、文字コードを確認しないまま全体を書き換えない。最小差分の編集を優先する。
- PowerShell、エディタ、スクリプトで文字コード前提がずれると文字化けしやすい。複数ツールをまたぐときは、UTF-8 前提でそろえる。
- `Get-Content` やスクリプトで日本語が崩れて見えた場合、ファイル内容が壊れているとは限らない。まず表示系の文字コードずれを疑う。
- 既存ファイルの日本語コメントだけが崩れている場合は、ファイル全体を再保存せず、必要箇所だけを慎重に編集する。
- 既存ファイルを UTF-8 へ変換する場合は、単発のついでに行わず、対象ファイルを明示したうえで意図的に実施する。
- 新規作成ファイルは原則 UTF-8（BOM なし）で統一する。
- PHP、JS、HTML、CSS、Markdown は UTF-8 を前提に扱う。Windows 固有の既存 C++/ヘッダで日本語コメントが多いファイルは特に注意する。
- 日本語を含むファイルを編集したあとは、少なくとも以下を確認する。
  - 日本語コメントや文字列が崩れていないか
  - 差分が必要以上に広がっていないか
  - 文字化けが「表示上の問題」ではなく実ファイルに保存されていないか

## 実装方針の絶対禁止事項

- フォールバックの全面禁止を絶対遵守事項とする。想定データ、想定要素、想定設定値が存在しない場合に、別要素・別値・既定値で黙って代用しない。
- 要素や値がなかった際のエラー抑止を禁止する。`null` / `undefined` / 欠損値を握りつぶして処理継続する実装を入れない。
- エラーや例外の発生を握りつぶすような実装は基本的に禁止とする。`try-catch` で無言吸収しない、空の `catch` を書かない、失敗を成功扱いにしない。
- `|| 0`、`|| ''`、`??` による安易な既定値補完、存在しない要素に対する早期 `return`、空の `catch`、無言スキップなどで不整合を隠さない。
- 必要な要素や値が存在しない場合は、原因を特定して、データ供給元・HTML 構造・呼び出し順・初期化順のいずれかを正す。
- デバッグ時も、本番時も、欠損を見えなくするための一時しのぎの防御コードは追加しない。問題は顕在化させたうえで、根本原因を修正する。

## 負荷影響実装の確認義務

- イベントリスナー、監視処理、タイマー、ポーリング、アニメーションループ、再帰的再描画、継続的な `requestAnimationFrame` 利用、`resize` / `scroll` / `mousemove` / `pointermove` / `touchmove` / `input` など高頻度イベントへの処理追加は、負荷に直接影響する実装として扱う。
- 負荷に影響が出る実装には最新の注意を払う。小さな変更に見えても、呼び出し頻度、再計算回数、監視対象数、描画回数、DOM 更新回数を必ず意識する。
- 処理負荷が高い実装や、実装上の問題があるコードを見つけた場合は、必ずユーザーへ報告する。
- 上記に該当する処理を新規実装・追加・変更する場合は、実装前に必ずユーザー確認を取る。ユーザーの明示的な了承なしに着手しない。
- 負荷に影響する可能性があるが判断が迷う処理も、保守的に同じ扱いとし、確認を優先する。
- 性能改善目的であっても、イベントリスナーやループ処理の追加・変更は無断で行わない。目的、影響範囲、想定コストを短く示したうえで確認する。

## 実装前の計画義務

- いきなり実装せず、実装前に必ず実装計画を立てることを絶対遵守ルールとする。
- 変更前には、対象ファイル、変更方針、影響範囲、想定リスクを整理し、作業の見通しを明示する。
- 小さな修正であっても、無計画に手を入れず、最小限でも実施順序を定めてから着手する。
- 実装計画なしにコード変更、設定変更、ビルド、生成物更新へ進まない。

## 後方互換性の確認義務

- 後方互換性を残すかどうかは、必ずユーザー確認を取る。勝手に互換レイヤー、旧仕様吸収、旧データ救済、旧 API 互換、旧 DOM 構造対応を実装しない。
- 互換性維持の有無で挙動、保守性、負債が変わる場合は、実装前に判断を仰ぐ。
- 「とりあえず壊れないようにしておく」目的の互換コードを無断で追加しない。必要性が明示されるまで、現行仕様を正として扱う。


## 実装方針

- コメントアウトは削除しない。
- C++ では、関数の宣言と定義の両方にデフォルト引数を指定しない。
- マジックナンバーの使用は極力避ける。必要な場合は確認する。
- 共通処理は必ず関数化・メソッド化し、重複を避ける。
- 不要コードを除去する場合、直接削除ではなく、可能な限り各行コメントアウトで対応する。
- ラムダ式の使用は避ける。
- 関数引数に別の関数を直接入れるような入れ子構造は避ける。
- 既存実装にこれらの方針と矛盾する仕組みがある場合は、独断で合わせ込まず確認を取る。

